app:
  port: 3000
  debug: true
  cache_ttl: 900
  max_file_size: 10

gcs:
  bucket_name: <bucket name>
  image_secret: <secret>
  service_account_email: <google access id>

database:
  host: localhost
  port: 5432
  name: rpkm-db
  username: root
  password: root

redis:
  host: localhost:6379
  password: ""